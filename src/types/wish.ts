export interface WishItem {
  wishId: number;
  title: string;
  itemName: string;
  brand: string;
  quantity: number;
  description: string;
  regDate: string; // ISO string
  claimTotalCount: number;
}
