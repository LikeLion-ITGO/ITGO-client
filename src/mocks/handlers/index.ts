import { storeHandlers } from "@/mocks/handlers/store";
// import { userHandlers } from "@/mocks/handlers/user";

// export const handlers = [...storeHandlers, ...userHandlers];
export const handlers = [...storeHandlers];
